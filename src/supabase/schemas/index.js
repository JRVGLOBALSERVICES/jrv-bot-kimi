const cars = require('./cars');
const catalog = require('./catalog');
const agreements = require('./agreements');
const dataStore = require('./data-store');

module.exports = { cars, catalog, agreements, dataStore };
